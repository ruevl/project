# 📚 Library Catalog API

REST API для управления библиотечным каталогом.

## ✨ Возможности
- CRUD операции с книгами
- Поиск и фильтрация
- Обогащение данных из Open Library (обложка, темы)
- Пагинация
- Валидация ISBN, года, страниц

## 🚀 Запуск
1. Установите зависимости:
   ```bash
   poetry install
   ```
2. Запустите PostgreSQL:
   ```bash
   docker-compose up -d postgres
   ```
3. Примените миграции:
   ```bash
   alembic upgrade head
   ```
4. Запустите сервер:
   ```bash
   uvicorn src.library_catalog.main:app --reload
   ```
5. Откройте: http://localhost:8000/docs

## 🛠 Технологии
FastAPI, PostgreSQL, SQLAlchemy 2.0 (async), Alembic, Pydantic v2
